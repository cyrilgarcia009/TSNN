# Core scientific stack
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65

# PyTorch — CPU version listed here; replace with appropriate CUDA wheel on GPU machines.
# AWS (CUDA 12): pip install torch --index-url https://download.pytorch.org/whl/cu121
# macOS (MPS):   pip install torch  (ships with MPS support)
torch>=2.1
torchvision

# Real-data experiment (M5 dataset)
neuralforecast>=1.7
datasetsforecast>=0.0.8

# Jupyter (optional — needed to run notebooks)
jupyterlab>=4.0
ipykernel>=6.20
ipywidgets
